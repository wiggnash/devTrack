info:
  name: get issue by id
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/issues/?id=1"
  params:
    - name: id
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
