info:
  name: get all issues
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/issues/all/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
