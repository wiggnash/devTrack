info:
  name: get all reports
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/reporters/all/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
